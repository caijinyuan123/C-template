#include<iostream>
#include<cstdio>
#include<algorithm>
#include<cstring>
#include<map>
using namespace std;

map <string ,int> h;
map <string ,int> ::iterator it;//迭代器

int main()
{
	/*
	string s = "abc";
	h[s] = 3;
	h.erase(s);//删除
	h.insert(make_pair(s,3));//添加
	h.count(s);//查找元素
	it=h.find(s);
	
	map的常见接口：
	map<k,v>				//定义
	insert(make_pair(k,v) )	//添加
	erase(k)				//删除
	count(k)				//是否存在
	find(k)					//查找至iterator
	size()					//map元素个数
	begin(),end()			//map的第一个和最后一个元素对应的iterator
	lower_bound(k)			//大于等于的第一个位置
	upper_bound(k)			//大于的第一个位置
	*/
    return 0;
}
